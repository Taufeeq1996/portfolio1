// Get the viewport width
var viewportWidth = window.innerWidth || document.documentElement.clientWidth;

// Log the viewport width to the console
console.log("Viewport width: " + viewportWidth + "px");
